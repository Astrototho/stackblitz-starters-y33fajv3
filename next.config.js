/** @type {import('next').NextConfig} */
const nextConfig = {
  // Plus besoin de "experimental", c'est géré nativement !
};

export default nextConfig; // ou module.exports = nextConfig; selon ton fichier
